server {
    listen 80 default_server;
    server_name jobs-numbers www.jobs-numbers;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/root/jobs-numbers-website/jobs_numbers.sock;
    }
}
